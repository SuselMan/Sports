/**
 * When true, all server sync and authentication are disabled.
 * The app runs fully offline using IndexedDB only.
 * Set to false to re-enable Google Auth + server sync.
 */
export const OFFLINE_ONLY = true;
